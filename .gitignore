# Node.js / NPM / Yarn
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json    # ignore nếu bạn không muốn lock version (tùy chọn, nhiều người vẫn commit)
yarn.lock            # ignore nếu không dùng yarn

# Environment variables / Secrets (rất quan trọng!)
.env
.env.local
.env.development
.env.test
.env.production
.env.*
!.env.example        # nếu có file mẫu thì giữ lại để người khác biết format

# Netlify / Serverless build artifacts
.netlify/
.netlify-cache/
.netlify/functions/*/node_modules/   # tránh commit sub node_modules nếu có

# Build / Dist / Output folders
dist/
build/
out/
public/dist/         # nếu có build frontend riêng

# Logs and temporary files
*.log
*.logs
logs/
tmp/
temp/
*.tmp
*.temp

# OS generated files
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor / IDE files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.code-workspace

# Database / Local data files (nếu từng dùng trước khi chuyển sang Mongo)
database.json
keys.json
*.sqlite
*.sqlite3
*.db

# Test / Coverage
coverage/
.nyc_output/
jest-coverage/

# Misc
*.swp
*~
.DS_Store
Thumbs.db